name: bio-exp-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - flask
      - gunicorn
      - pymupdf
      - sentence-transformers
      - transformers
      - torch
      - scikit-learn
      - numpy
      - tqdm
      - pydantic
      - paddleocr
      - pillow
